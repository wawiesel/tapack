!! Get $R^{1}$ equivalent of $I^{1}$ SCLS.
real_SCLS =  REAL(SCLS,KIND_R)

!! Get $R^{1}$ equivalent of $I^{1}$ SCL.
real_SCL  = REAL(SCL,KIND_R)

!! Call the real routine.
loc = NEARLOC(real_SCLS,real_SCL)